// window.addEventListener('load', function() {
    // const loaderOverlay = document.querySelector('.loader-overlay');
    // loaderOverlay.classList.add('loaded');
// });

var type = new Typed('#js_web', {
    strings:["Front-end" , "YouTuber" , "Mobile Developer" , "UI/UX"],
    typeSpeed:100,
    backSpeed:100,
    backDelay:1000,
    loop:true
});
var type = new Typed('#wecl_js', {
    strings:["TO" , "My" , "Portfolio_Page"],
    typeSpeed:100,
    backSpeed:100,
    backDelay:1000,
    loop:true
});

  